import 'package:flutter/material.dart';

class FullLifeCicleControllerPage extends StatelessWidget {
  const FullLifeCicleControllerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Page'),
      ),
      body: Container(),
    );
  }
}
