import 'package:flutter/material.dart';

class GetxControllerExamplePage extends StatelessWidget {
  const GetxControllerExamplePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
