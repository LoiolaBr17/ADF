import 'package:flutter/material.dart';
import 'package:get/get.dart';

class TiposObsPage extends StatelessWidget {
  final nome = "mateus".obs;

  TiposObsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
