import 'package:flutter/material.dart';

class OfHomePage extends StatelessWidget {
  const OfHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Off Home Page'),
      ),
      body: Container(),
    );
  }
}
